# Execution Queue

Read this file before scanning the rest of `docs/`. It is the dispatch source of truth for next-batch execution.

## Ready Now

`Ready Now` should contain implementation-first entries by default. `kind: docs` or `kind: coordination` items belong here only when they are the smallest verified unblocker for the next implementation batch.

| id | title | priority | size | kind | autonomous_ready | depends_on | plan | plan_section | target_paths | deliverable | verification | last_verified |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

Current executor status (`2026-06-24`): `Ready Now` is empty after
`JN-RUNTIME-INTERPRETER-LEGACY-CLOSURE-001` closed the legacy interpreter
blocker. Use `Next Curation Target` before promoting new implementation work.

## Next Curation Target

Use this section when `Ready Now` has no executable entry, or when a
coordination batch explicitly pre-seeds post-child candidates behind the current
ready row. Keep it to 1-3 promotion candidates, ordered by the recommended next
promotion. A candidate is not a queue row yet; the next curation pass should
either promote it into `Ready Now` with matching plan frontmatter or replace it
with a better source-backed candidate.

| blocked_id | candidate_child_id | kind | source_contract | why_next | target_paths | verification | promotion_check |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `JN-MAP-FILTER-COMPAT-PLAN-001` | `JN-MAP-FILTER-COMPAT-CLOSURE-001` | `coordination` | [blocker-contracts.md#jn-map-filter-compat-plan-001](blocker-contracts.md#jn-map-filter-compat-plan-001) | Active `jazz-next` already implements function-first `map` and `filter`; the remaining legacy cleanup plan is a compatibility/docs decision, so the next curation pass should close stale migration scope or replace it with a concrete docs-only target. | `docs/plans/spec-cleanup/2026-03-02/decisions/02-map-filter-order.md`, `README.md`, `docs/execution/queue.md` | `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`; `git diff --check` | Confirm active examples do not require collection-first compatibility, and do not add parser aliases or runtime behavior changes. |

## Blocked

| id | title | blocked_on | reason | plan | last_verified |
| --- | --- | --- | --- | --- | --- |
| `JN-MODULE-REBASE-PLAN-001` | Rebase module/import execution plan onto active `jazz-next` owners | `Concrete future stdlib/catalog growth contract beyond the closed current module/prelude harness` | Active-path ownership is now rebased onto `ModuleResolver.hs`, `Driver.hs`, `CLI/Main.hs`, and loader/module tests. Dependency-module expression isolation, explicit symbol-list visibility, alias-import unqualified visibility, `Alias::symbol` qualified alias lookup, default bundled-prelude module graph helpers, bundled-prelude reproducibility coverage, and explicit no-prelude module graph ownership coverage are landed. Current module/import and stdlib execution semantics are closed for the active subset; any further stdlib/catalog growth still needs a concrete API/runtime contract before returning to `Ready Now`. | [2026-03-18-jazz-next-runtime-architecture-and-interpreter-execution-plan.md](../plans/2026-03-18-jazz-next-runtime-architecture-and-interpreter-execution-plan.md) | `2026-06-01` |
| `JN-USER-DEFINED-OPERATORS-PLAN-001` | Stage user-defined operator work beyond frozen v1 operators | `Runtime operator semantics remain separate` | The Stage 2 fixed-tier declaration parser child is complete for parser declaration recognition, source-unit-local metadata, fixed-tier parsing, and diagnostics only. Runtime operator semantics, runtime overload dispatch, custom precedence, custom associativity, and new built-in operators remain blocked until separate executable contracts exist. | [15-operator-fixity-and-sections.md](../plans/spec-clarification/2026-03-03/syntax/15-operator-fixity-and-sections.md) | `2026-06-04` |
| `JN-TYPE-GRAMMAR-CLOSURE-PLAN-001` | Plan type-system expansion beyond rebase closure | `Remaining solver contract slices need verifier-backed child rows` | The broad type-solver coordination contract is accepted and recorded as `JN-TYPE-SOLVER-CONTRACT-001`, and the ordinary-binding schemes/per-use instantiation child has landed. Inferred class constraints, broad defaulting, solver-backed constrained signatures, runtime dispatch, explicit type application, and the remaining solver slices stay blocked until their own verifier-backed child rows are promoted. | [2026-03-18-jazz-next-type-grammar-and-signature-rebase-plan.md](../plans/2026-03-18-jazz-next-type-grammar-and-signature-rebase-plan.md) | `2026-06-24` |
| `JN-PATTERN-FUTURE-FORMS-PLAN-001` | Plan future pattern forms beyond the active subset | `Or-patterns and pattern synonyms need separate contracts` | Literal, wildcard, variable, constructor, exact-length list, cons-like list, fixed-arity tuple, as-patterns, and single `if` guards now execute in `case` arms, and lambda parameters keep the active pattern subset without guards. Or-patterns and pattern synonyms remain blocked until they have their own binder/type/runtime contracts, target paths, and focused verification. | [2026-03-18-jazz-next-adt-and-pattern-matching-rebase-plan.md](../plans/2026-03-18-jazz-next-adt-and-pattern-matching-rebase-plan.md) | `2026-06-24` |
| `JN-PRIMITIVE-SURFACE-EXPANSION-PLAN-001` | Plan primitive semantics expansion beyond the v1 runtime subset | `Post-suffix primitive deltas need accepted implicit-promotion, mixed-width, or solver contracts` | The v1 primitive/runtime subset is implemented for integer arithmetic, same concrete `Float`/`Float16`/`Float32`/`Float64` arithmetic, same concrete `Float`/`Float16`/`Float32`/`Float64` comparison/equality, structural list/tuple equality when nested element types are equality-supported, structural ADT equality when every declared constructor payload type is equality-supported, callable equality/inequality rejection, equality/inequality, `map`, `filter`, `hd`, `tl`, `print!`, explicit target-named numeric conversions through the active prelude/kernel boundary, numeric width signature ownership, default Float64 fractional literal values, direct annotated `Float16`/`Float32` fractional literal bindings, and parser-owned lowercase `f16`/`f32`/`f64` fractional literal suffixes. Post-suffix primitive work such as implicit integer-to-float promotion, implicit mixed-width behavior, and broader numeric solver work remains blocked until separate concrete contracts are accepted. | [16-primitive-semantics-contract.md](../plans/spec-clarification/2026-03-03/runtime/16-primitive-semantics-contract.md) | `2026-06-04` |
| `JN-RUNTIME-PRODUCTIZE-CLOSURE-PLAN-001` | Productize interpreter-first execution beyond docs closure | `Additional runtime product delta beyond compile/run/help baseline` | The runtime/driver active path now has diagnostic-only compile results, run-mode execution, module graph helpers, bundled-prelude reproducibility coverage, and explicit CLI help output. The interpreter-first compile/run/help baseline is closed. Additional runtime product work remains blocked until another concrete delta is accepted. | [2026-03-18-jazz-next-runtime-architecture-and-interpreter-execution-plan.md](../plans/2026-03-18-jazz-next-runtime-architecture-and-interpreter-execution-plan.md) | `2026-06-24` |
| `JN-MAP-FILTER-COMPAT-PLAN-001` | Finish map/filter compatibility and migration policy | `Compatibility-mode decision or docs-only migration scope` | Active `jazz-next` already implements function-first `map` and `filter` behavior with type/runtime coverage and bundled-prelude aliases. The remaining legacy cleanup plan asks for compatibility-policy and docs migration decisions, not a concrete active-path compiler implementation batch. | [02-map-filter-order.md](../plans/spec-cleanup/2026-03-02/decisions/02-map-filter-order.md) | `2026-05-22` |
| `JN-PARSE-ONLY-LEGACY-REBASE-001` | Rebase parse-only feature cleanup away from `jazz-hs` | Active `jazz-next` feature-resolution matrix | The parse-only cleanup plan targets legacy `jazz-hs` parser/analyzer/codegen files and a Nix matrix for that compiler. Under the current workspace policy, new compiler behavior belongs in `jazz-next`; this plan needs an active-path replacement matrix before any executor-safe implementation row can be created. | [06-parse-only-features-resolution.md](../plans/spec-cleanup/2026-03-02/compiler/06-parse-only-features-resolution.md) | `2026-05-22` |
| `JN-PURITY-EFFECT-TYPING-PLAN-001` | Plan full purity/effect typing beyond stub-v1 enforcement | `Concrete effect-system contract beyond bang-suffix stub` | Stub-v1 purity enforcement and `print!` support are implemented and verified in `jazz-next`. The remaining broader effect work is explicitly out of scope in the plan, including higher-order purity, effect types, cross-module purity graphs, and runtime enforcement, so it needs a new semantics contract before any implementation row is safe. | [03-purity-bang-semantics.md](../plans/spec-cleanup/2026-03-02/decisions/03-purity-bang-semantics.md) | `2026-05-23` |
| `JN-ABSTRACTION-SEMANTICS-PLAN-001` | Plan class/impl abstraction semantics beyond parser boundaries | `Future dictionary/default-method/superclass/solver/module-method contracts` | Active `jazz-next` now parses and lowers canonical `class` declarations with explicit lowercase parameter metadata plus signature-only method metadata, concrete `impl` declarations with method binding metadata, rejects duplicate class method signatures with `E1006`, rejects duplicate impl method bindings with `E1007`, rejects class method body/default syntax, rejects non-binding or non-concrete method-bearing impl bodies, rejects duplicate class names and duplicate concrete impl facts, validates concrete constrained signatures against known class/impl facts using declared class arity, executes explicit `Class::method` references by visible concrete impl method bodies including typed selection across multiple concrete candidates, validates concrete impl method bodies against the substituted class method signature, exposes bundled-prelude `Eq(Int).equals` and `Eq(Bool).equals` method bodies through default prelude loading, and permanently rejects non-canonical `trait` declarations. Dictionaries, default methods, superclasses, overlap/orphans beyond duplicate visible facts, inferred constraints, broader bundled-prelude method families, module export/import behavior for methods, and broader runtime evidence remain blocked until separate executable contracts define syntax, target paths, and focused verification. | [01-authoritative-syntax.md](../plans/spec-cleanup/2026-03-02/decisions/01-authoritative-syntax.md) | `2026-06-04` |

## Done

Recent completed items may stay here briefly while review is active. Move stable closure evidence to [done-archive.md](done-archive.md) so this file remains a dispatcher.

| id | closure evidence | completed_on |
| --- | --- | --- |
| `JN-RUNTIME-INTERPRETER-LEGACY-CLOSURE-001` | Closed the legacy Haskell interpreter implementation plan as reference-only. Active interpreter-backed execution stays routed through the `jazz-next` runtime architecture plan and runtime product blocker; no `jazz-hs` implementation, second runtime path, or active compile/run/help contract change was promoted. Verification: `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`; `git diff --check`. | `2026-06-24` |
| `JN-BACKEND-TARGET-LEGACY-CLOSURE-001` | Closed the legacy backend-target strategy plan as reference-only. Active runtime product work stays routed through the `jazz-next` runtime architecture plan and the runtime product blocker; no backend implementation, legacy runtime edits, generated-artifact policy, or alternate codegen target was promoted. Verification: `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`; `git diff --check`. | `2026-06-24` |
| `JN-TRAIT-CLASS-LEGACY-CLOSURE-001` | Closed the legacy trait/class cleanup path as reference-only. Active abstraction declaration syntax remains canonical `class`/`impl`; declaration-shaped `trait` syntax stays permanently rejected in `jazz-next`; no compatibility alias, deprecation warning, or W0004 path was added; and future abstraction work must use active `jazz-next` contracts rather than read-only `jazz-hs` parser/prelude/test edits. Verification: `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Parser/ParserFoundationSpec.hs`; `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`; `git diff --check`. | `2026-06-24` |
| `JN-WARNING-W0004-RESERVED-CLOSURE-001` | Closed the current `deprecated-syntax` / `W0004` decision as reserved-only. `DeprecatedSyntax` remains stable warning metadata with code `W0004`, token `deprecated-syntax`, and no analyzer emitter; warning config parsing does not imply emission; `trait` declarations remain parser-rejected unsupported syntax; and future W0004 work requires a new accepted active syntax surface with payload, target paths, and focused verification. Verification: `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Config/WarningConfigSpec.hs`; `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Parser/ParserFoundationSpec.hs`; `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`; `git diff --check`. | `2026-06-24` |
| `JN-WARNING-DEPRECATED-SYNTAX-CONTRACT-001` | Decided the current W0004 contract: there is no accepted active-path parser or analyzer surface that is both implemented and deprecated enough to warn on, and non-canonical `trait` declarations are parser errors rather than compatibility syntax. The policy is reserved-only until a future accepted active syntax surface is intentionally deprecated. | `2026-06-24` |
| `JN-WARNING-REMAINING-EMITTERS-PLAN-001` | Closed the remaining deprecated-syntax emitter blocker for the current language surface. W0001-W0003 are the active analyzer emitters; W0004 stays metadata-only and any future emitter must start from a new accepted-surface contract instead of a broad implementation row. | `2026-06-24` |
| `JN-RUNTIME-CLI-HELP-001` | Implemented explicit `--help` and `-h` CLI usage output in `jazz-next/src/JazzNext/CLI/Main.hs` with preflight behavior before argument validation, env lookup, source/config/prelude/module reads, or module graph file reads. Added focused `CLISpec` coverage and documented help usage in `jazz-next/README.md`. Verification: `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/CLI/CLISpec.hs`; `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`; `git diff --check`. | `2026-06-24` |
| `JN-RUNTIME-CLI-PRODUCT-DELTA-CONTRACT-001` | Locked the next runtime product delta as CLI help output only: recognize `--help` and `-h`, print stable usage to stdout with exit `0`, keep stderr empty, preempt source/config/prelude/module reads, avoid a bare `help` subcommand so positional source paths remain intact, and keep compile/run/module/prelude/warning semantics unchanged. Promoted implementation child `JN-RUNTIME-CLI-HELP-001`. Verification: `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`; `git diff --check`. | `2026-06-24` |
| `JN-PATTERN-GUARD-SEMANTICS-001` | Implemented optional `if` guard expressions on `jazz-next` case arms across surface/core AST, parser/lowering, analyzer/module/driver/recursive-binding traversal, type inference, and runtime evaluation. Pattern binders are visible to guards, guards typecheck as `Bool`, `False` falls through to later arms, pattern failure skips guard evaluation, and `E3022` remains the no-selected-arm runtime diagnostic. Verification: `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Parser/AdtPatternParserSpec.hs`; `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Semantics/AdtPatternTypeSpec.hs`; `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Semantics/AdtPatternRuntimeSpec.hs`; `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Modules/LoaderSpec.hs`; `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`; `git diff --check`. | `2026-06-24` |
| `JN-PATTERN-GUARD-CONTRACT-001` | Locked guard-only case-arm semantics in `docs/spec/pattern-matching-semantics.md`, `docs/spec/adt-pattern-semantics.md`, and the active ADT/pattern plan: a case arm may use existing `if` as the guard introducer before the body arrow, pattern binders are visible to the guard, guards introduce no binders, non-`Bool` guards are compile-time diagnostics, runtime evaluates guards only after pattern success, `False` falls through, and `E3022` remains the no-match diagnostic. Promoted implementation child `JN-PATTERN-GUARD-SEMANTICS-001`. Verification: `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`; `git diff --check`. | `2026-06-24` |
| `JN-TYPE-SOLVER-ORDINARY-BINDING-SCHEMES-001` | Added ordinary user-binding type schemes in `jazz-next/src/JazzNext/Compiler/TypeInference.hs`, fresh per-use instantiation for generalized non-recursive and completed recursive-group bindings, and guards preserving direct constructor aliases, adjacent signatures, monomorphic constrained signatures, constrained numeric/equality variables, and existing generic ADT constructor schemes. Added focused coverage in `BindingSignatureCoherenceSpec.hs` and `AdtPatternTypeSpec.hs`. Verification: `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Semantics/BindingSignatureCoherenceSpec.hs`; `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Semantics/AdtPatternTypeSpec.hs`; `bash jazz-next/scripts/test-warning-config.sh`; `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`; `git diff --check`. | `2026-06-24` |
| `JN-TYPE-SOLVER-CONTRACT-001` | Locked one broad solver semantics agreement for ordinary user-binding generalization, per-use instantiation of generalized schemes, recursive-group generalization, inferred class constraints against visible class/impl facts, final defaulting and ambiguity rules that preserve current numeric literal defaults and do not reopen primitive mixed-width behavior, solver-backed constrained signatures, and deterministic diagnostics for unsolved constraints, duplicate constraints, arity errors, unsupported type applications, and un-defaulted variables. Promoted implementation child `JN-TYPE-SOLVER-ORDINARY-BINDING-SCHEMES-001`. Verification: `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`. | `2026-06-04` |
| `JN-PRIMITIVE-FRACTIONAL-LITERAL-SUFFIXES-001` | Implemented parser-owned lowercase `f16`/`f32`/`f64` suffixes on existing decimal fractional literals in `jazz-next`, resolving directly to `Float16`/`Float32`/`Float64`, preserving finite-target rounding/overflow behavior, no-prelude/import independence, prelude-owned conversion APIs, and existing mixed-width rejection. Verification: `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Parser/ParserFoundationSpec.hs`; `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Semantics/PrimitiveSemanticsSpec.hs`; `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Semantics/BindingSignatureCoherenceSpec.hs`; `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Semantics/RuntimeSemanticsSpec.hs`. | `2026-06-04` |
| `JN-PRIMITIVE-LITERAL-SUFFIX-CONTRACT-001` | Locked parser-owned lowercase `f16`/`f32`/`f64` suffixes attached directly to existing decimal fractional literals, accepted only for `Float16`/`Float32`/`Float64`, independent of imports/no-prelude mode, with direct concrete-width resolution, finite-target rounding/overflow reuse, prelude-owned `toFloat16`/`toFloat32`/`toFloat64` conversions preserved, and existing mixed-width rejection retained. Promoted implementation child `JN-PRIMITIVE-FRACTIONAL-LITERAL-SUFFIXES-001`. Verification: `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`. | `2026-06-04` |
| `JN-OPERATORS-STAGE2-FIXED-TIER-PARSER-001` | Implemented parser-only Stage 2 fixed-tier operator declarations in `jazz-next`: top-level `operator <symbol> tier <1-5>.` declarations are consumed as source-unit-local parser metadata, valid declared uses parse as infix/operator-value/section nodes after declaration, invalid symbols/reserved or built-in operators/invalid tiers/duplicates/nested declarations/use-before-declaration are diagnosed, and no runtime semantics were added. Verification: `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Parser/OperatorFixitySpec.hs`; `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Parser/OperatorInvalidSyntaxSpec.hs`; `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`; `git diff --check`. | `2026-06-04` |
| `JN-OPERATORS-STAGE2-FIXED-TIER-CONTRACT-001` | Locked Stage 2 user-defined operator declarations as explicit top-level `operator <symbol> tier <1-5>.` metadata with fixed-tier associativity inheritance, a concrete ASCII operator symbol set, reserved/built-in/comment-token exclusions, before-use source-unit visibility, and explicit non-goals for runtime overload dispatch, new built-ins, custom precedence, and custom associativity. Promoted parser-only child `JN-OPERATORS-STAGE2-FIXED-TIER-PARSER-001`. Verification: `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`. | `2026-06-04` |
| `JN-ABSTRACTION-PRELUDE-EQ-BOOL-METHOD-001` | Added exactly one bundled-prelude `impl Eq(Bool).equals` body in generated source and the checked-in prelude mirror, with default-prelude Bool dispatch coverage plus no-prelude and explicit-prelude isolation checks. Verification: `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Modules/PreludeLoadingSpec.hs`; `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Semantics/BindingSignatureCoherenceSpec.hs`; `bash jazz-next/scripts/runghc.sh -i./jazz-next/src -i./jazz-next/test jazz-next/test/JazzNext/Compiler/Semantics/RuntimeSemanticsSpec.hs`; `bash scripts/check-execution-queue.sh`; `bash scripts/check-docs.sh`. | `2026-06-04` |

## Entry Template

```md
| `ITEM-ID` | Short task name | `P1` | `S` | `impl` | `yes` | `-` | [linked-plan.md](../plans/example.md) | `Task 2` | `jazz-next/src/JazzNext/Compiler/Foo.hs`, `jazz-next/test/JazzNext/Compiler/FooSpec.hs` | Concrete implementation deliverable. | Exact verification command(s). | `YYYY-MM-DD` |
```
