module Main {
  import Collections.
  import Dictionary.
  import Map.
  import Queue.
  import Set.

  (dictionaryToList wordCounts, dictionaryLookup wordCounts "parse",
   mapToList orderedValues, setToList selectedValues,
   queueToList pendingValues, queuePeek pendingValues).
}
