module Main {
  import List.
  import Queue.
  import Traversal.

  original = queueSingleton 1.
  visited = breadthFirst 1.
  (visited, queueToList original, listLength visited,
   listFoldLeft (\(total, item) -> total + item) 0 visited).
}
