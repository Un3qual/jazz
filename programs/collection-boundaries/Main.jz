module Main {
  import Collections.
  import Dictionary as Dictionary.
  import Map as Map.
  import Queue as Queue.
  import Set as Set.

  (Dictionary::toList wordCounts, Dictionary::lookup wordCounts "parse",
   Map::toList orderedValues, Set::toList selectedValues,
   Queue::toList pendingValues, Queue::peek pendingValues).
}
