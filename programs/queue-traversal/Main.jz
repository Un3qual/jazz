module Main {
  import List as List.
  import Queue as Queue.
  import Traversal.

  original = Queue::singleton 1.
  visited = breadthFirst 1.
  (visited, Queue::toList original, List::length visited,
   List::foldLeft (\(total, item) -> total + item) 0 visited).
}
